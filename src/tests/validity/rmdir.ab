import * from "std/fs"

main {
    const tmpdir = temp_dir_create("amber-XXXX", true, true)?
    trust $ touch {tmpdir}/a $
    rmdir("{tmpdir}")
    if not dir_exists("{tmpdir}") {
        echo("Succeeded")
    } else {
        echo("File still exists")
    }
}