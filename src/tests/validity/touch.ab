import * from "std/fs"
import { shell_name } from "std/env"

main {
    const tmpdir = temp_dir_create("amber-XXXX", true, true)?
    touch("{tmpdir}/a")
    if file_exists("{tmpdir}/a") {
        echo("Succeeded")
    } else {
        echo("Not Found")
    }
}
