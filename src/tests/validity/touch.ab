import * from "std/fs"

main {
    const tmpdir = temp_dir_create("amber-XXXX", true, true)?
    trust touch("{tmpdir}/a")
    if file_exists("{tmpdir}/a") {
        echo("Succeeded")
    } else {
        echo("Not Found")
    }
}
