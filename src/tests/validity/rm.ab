import * from "std/fs"
main {
    let tmpdir = unsafe $mktemp -d /tmp/amber-XXXX$
    unsafe $touch {tmpdir}/a$
    unsafe rm "{tmpdir}/a" 
    if not file_exist("{tmpdir}/a") {
        echo "Succeded"
    } else {
        echo "Found"
        unsafe $rm -fr {tmpdir}$
    }
}
