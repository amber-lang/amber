import * from "std/fs"

// Output
// a
// b
// c

main {
    const tmpdir = temp_dir_create("amber-XXXX", true, true)?
    trust $ touch {tmpdir}/a $
    trust $ touch {tmpdir}/b $
    trust $ touch {tmpdir}/c $
    for file in ls(tmpdir)? {
        echo(file)
    }
}