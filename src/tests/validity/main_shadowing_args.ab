// Output
// Succeeded
// 1

// Argument names in different functions can be the same
fun func1(args: Bool): Text {
    if args:
        return "Succeeded"
}

main(args) {
    echo(func1(true))
    echo(len(args))
}
