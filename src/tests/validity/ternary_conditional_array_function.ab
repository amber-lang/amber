// Output
// This
// is
// an
// array

fun foo(val) {
    for item in val {
        echo(item)
    }
}

const property = 25
foo(property > 10 then ["This", "is", "an", "array"] else ["This", "should", "never", "happen"])
