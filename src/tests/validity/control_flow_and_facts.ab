// Output
// 30
// Passed

fun test_and(a: Text | Int, b: Text | Int) {
    if a is Int and b is Int {
        const result = a + b
        echo(result)
    }

    if a is Int and b > 30 {
        echo("Failed")
    } else {
        echo("Passed")
    }
}
test_and(10, 20)
