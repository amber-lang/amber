import { pgrep, kill } from "std/env"

main {
    trust $ yes > /dev/null 2>&1 & $
    const yes_pid = pid()
    disown(yes_pid)
    kill(yes_pid, "KILL")?
    if len(pgrep("yes")) == 0 {
        echo("Succeeded")
    } else {
        echo("Process still alive")
    }
}