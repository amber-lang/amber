// Output
// Outer: 42
// If: 12
// Else: 24

main {
    const x = 42
    echo "Outer: {x}"

    if x > 12 {
        const x = 12
        echo "If: {x}"
    }

    if x < 12 {
        const x = 12
        echo "If: {x}"
    } else {
        const x = 24
        echo "Else: {x}"
    }
}