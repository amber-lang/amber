// Output
// Succeeded

fun test_failable(): Num? {
    if 1 < 5 {
        fail 1
    }
    return 42
}

main {
    test_failable() succeeded {
        echo "This should not print!"
    }
    echo "Succeeded"
}