import { parse_int } from "std/text"

main {
    trust $ SECONDS="0" $
    sleep(2)?
    const seconds = trust $ printf '%s' "\$SECONDS" $
    if parse_int(seconds)? >= 2 {
        echo("Succeeded")
    } else {
        echo("No Sleep")
    }
}