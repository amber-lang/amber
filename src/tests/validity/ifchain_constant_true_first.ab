// Test ifchain with constant-true first condition - else should be pruned
// Output [bash]
// A
// Output [zsh]
// B
// Output [ksh]
// B

if shellname() == "bash" {
    echo("A")
} else {
    echo("B")
}
