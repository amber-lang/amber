
main {
    trust $ sudo() \{ command sudo -n "\$@"; } $
    
    // Use sudo with suppress modifier
    sudo suppress $ true $ exited(code): if {
        code == 0:
            echo("Succeeded")
        else:
            echo("Succeeded")
    }
}