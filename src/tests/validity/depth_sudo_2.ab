
main {
    trust $ sudo() \{ command sudo -n "\$@"; } $
    
    // Use sudo with suppress modifier
    sudo suppress $ id -u $ exited(code): if {
        code == 0:
            echo("Succeeded")
        else:
            echo("Succeeded")
    }
}