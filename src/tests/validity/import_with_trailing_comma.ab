import {
    replace,
    replace_regex,
} from "std/text"
import { sum, abs } from "std/math"
import {} from "std/env"

// Output
// c
// 3

echo replace_regex(replace("aa", "a", "b"), "b+", "c", true)
echo sum([abs(-2), 1])
