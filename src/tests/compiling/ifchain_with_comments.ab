const x = 1
if {
    // Check for large values
    x > 10: {
        echo "large"
    }
    // Then for the second check
    // Check for medium values
    x > 5: {
        echo "medium"
    }
    // Otherwise it's small
    else: {
        echo "small"
    }
}