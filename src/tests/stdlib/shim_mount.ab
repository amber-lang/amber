import * from "std/env"
// Output
// mount called with args: /dev/sda1 /mnt/test


main {
    // Setup: prepend shims directory to PATH
    env_var_set("PATH", "src/tests/utils/shims:" + env_var_get("PATH")?)?
    mount("/dev/sda1", "/mnt/test")?
}
