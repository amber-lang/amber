import { shopt_enable, shell_name } from "std/env"

// Output
// hello
// world
// *

main {
   if shell_name() == "bash" {
      shopt_enable("xpg_echo")?
      trust $ echo "hello\\nworld" $
      shopt_enable("noglob", true)?
      trust $ echo * $
   } else {
      echo("Succeeded")
   }
}