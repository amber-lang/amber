import { shopt_enable } from "std/env"

// Output
// hello
// world
// *

main {
   if shellname() == "bash" {
      shopt_enable("xpg_echo")?
      trust $ echo "hello\\nworld" $
      shopt_enable("noglob", true)?
      trust $ echo * $
   } else {
      echo("Succeeded")
   }
}
