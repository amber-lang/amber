import * from "std/array"

// Output
// Not Found

main {
    let _array = ["apple", "banana cherry"]
    if includes(_array, "banana") {
        echo "Found"
    } else {
        echo "Not Found"
    }
}
