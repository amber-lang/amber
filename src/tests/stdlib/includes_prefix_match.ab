import * from "std/array"

// Output
// Not Found

main {
    let array = ["apple", "banana cherry"]
    if includes(array, "banana") {
        echo "Found"
    } else {
        echo "Not Found"
    }
}
