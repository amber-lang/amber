import { shopt_disable, shell_name } from "std/env"

// Output
// .

main {
   if shell_name() == "bash" {
      shopt_disable("dotglob")?
      trust $ echo . $
      shopt_disable("noglob", true)?
   } else {
      echo("Succeeded")
   }
}