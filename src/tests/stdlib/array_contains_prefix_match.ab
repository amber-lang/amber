import * from "std/array"

// Output
// Not Found

main {
    const array = ["apple", "banana cherry"]
    if array_contains(array, "banana") {
        echo("Found")
    } else {
        echo("Not Found")
    }
}
