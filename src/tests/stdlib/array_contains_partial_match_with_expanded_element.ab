import * from "std/array"

// Output
// Not Found

main {
    const array = ["foo", "bar", "baz"]
    if array_contains(array, "oo ba") {
        echo("Found")
    } else {
        echo("Not Found")
    }
}
