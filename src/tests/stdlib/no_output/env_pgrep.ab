import { pgrep } from "std/env"

main {
    // Search for bash processes (should exist since we're running in bash)
    let pids = pgrep("bash")
    if len(pids) > 0 {
        echo("Succeeded")
    }
}
