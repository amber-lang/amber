import { make_executable } from "std/fs"

// Output
// created

main {
    let tmpdir = trust $mktemp -d /tmp/amber-XXXX$
    trust $touch {tmpdir}/amber-symbolic$
    trust $touch /tmp/amber-symbolic$
    if make_executable("/tmp/amber-symbolic") {
        echo "created"
    }
    trust $rm {tmpdir}/amber-symbolic$
} 
