import * from "std/array"

main {
    const array = ["apple", "banana cherry"]
    if array_contains(array, "banana cherry") {
        echo("Succeeded")
    } else {
        echo("Not Found")
    }
}
