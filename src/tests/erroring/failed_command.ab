// Output
// The command can potentially fail. Use '?', 'failed', 'succeeded', or 'exited' to manage its result.

echo($ echo "Hello, this will not fail" $)
echo($ echo "This is my first amber command" $)
