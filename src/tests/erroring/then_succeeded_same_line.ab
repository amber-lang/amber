// Output
// Cannot use both 'exited' and 'succeeded' blocks for the same command

main {
    $ echo "test" $ exited(code) {
        echo("Success!")
    } succeeded {
        echo("Success!")
    }
}
